apiVersion: argoproj.io/vqalpha1
kind: Application
metadata:
    name: dev-root-application
spec:
    source:
        repoURL: https://github.com/your-org/gitops.git
        targetRevision: main
        path: architectures/AppOfApps/dev
    destination:
        server: https://dev-kubernetes.default.svc
        namespace: argocd

    syncPlicy:
        automated:
            prune: true
            selfHeal: true
        
        syncOptions:
            - CreateNamespace=true
