{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
    name: {{ include "eks-python-app.serviceAccountName" .}}
    labels:
        {{- include "eks-python-app.labels" . | nindent 4 }}
{{- end }}